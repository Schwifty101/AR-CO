{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm turbo run build --filter=web",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://ar-co-production.up.railway.app/api/:path*"
    }
  ]
}