[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @repo/shared build && pnpm --filter api build"
watchPatterns = ["/apps/api/**"]

[deploy]
startCommand = "cd apps/api && node dist/main"
