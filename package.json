{
  "name": "ar-co",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.28.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "cloudinary": "^2.9.0",
    "turbo": "^2.3.3"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}