# Enforce pnpm usage only
engine-strict=true

# Flat node_modules so Next.js can resolve tailwindcss/postcss from pnpm's virtual store
shamefully-hoist=true

# Lock file configuration
lockfile=true
prefer-frozen-lockfile=false

# Auto install peers
auto-install-peers=true

# Strict peer dependencies
strict-peer-dependencies=false
